import React from 'react'
import { Text, View } from 'react-native'

export const ChatScreen = () => {
  return (
    <View style={{flex:1,justifyContent:"center",alignItems:'center'}}>
      <Text style={{fontSize:25, fontWeight:'bold'}}>Chat Screen</Text>
    </View>
  )
}
